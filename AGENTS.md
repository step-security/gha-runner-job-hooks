# Repository Instructions

- Follow a top-down file structure: keep primary exported logic near the top of a file, and place helper functions near the bottom.
- When adding new helpers during edits, prefer moving them below the main flow instead of interleaving them with the primary logic.
